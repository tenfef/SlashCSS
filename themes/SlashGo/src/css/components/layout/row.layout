/************************************************************
GRID
************************************************************/

$row-gutters: 5 !default;

.row {

	clear: both;
	display: flex;
	flex-direction: row;
	flex-wrap: wrap;

	/******************************
  MEDIA
  ******************************/

	&\:media {
		display: inline-flex;
		vertical-align: middle;
		& > [class^=column] {
			display: flex;
			flex-grow: 0;
		  flex-basis: auto;
			align-items: center;
			width: auto;
		}
	}

	/******************************
  GUTTER
  ******************************/

	&-gutter {
		@while $row-gutters > 0 {
			&\:#{$row-gutters} {
				margin-left: $row-gutters * -1rem / 2;
				margin-right: $row-gutters * -1rem / 2;
				& > [class^=column] {
					padding-left: $row-gutters * 1rem / 2;
					padding-right: $row-gutters * 1rem / 2;
				}
			}
			$row-gutters: $row-gutters - 1;
		}
		@each $key, $value in $screens {
			&\@#{$key} {
				@media (min-width : #{$value}) {
					@while $row-gutters > 0 {
						&\:#{$row-gutters} {
							margin-left: $row-gutters * -1rem / 2;
							margin-right: $row-gutters * -1rem / 2;
							& > [class^=column] {
								padding-left: $row-gutters * 1rem / 2;
								padding-right: $row-gutters * 1rem / 2;
							}
						}
						$row-gutters: $row-gutters - 1;
					}
				}
			}
		}
	}
}
